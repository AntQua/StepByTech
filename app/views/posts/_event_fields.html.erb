<div class="form-group">
  <%= f.select :event_id,
      @events.map { |e| [e.title, e.id] },
      { include_blank: "Seleccione um Evento", selected: @post.event_id },
      class: "form-select",
      data: { post_form_target: "eventSelect" } %>
</div>
