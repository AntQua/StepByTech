<div class="form-group">
  <%= f.select :program_id,
               @programs.map { |p| [p.title, p.id] },
               { include_blank: "Seleccione um Programa" },
               class: "form-select",
               data: { post_form_target: "programSelect" } %>
</div>
