<div class="d-flex justify-content-between align-items-center mb-4 bg-white rounded-4 p-3">
  <h2>Programs</h2>
  <%= link_to 'New Program', new_program_path, class: 'btn btn-primary rounded-4' %>
</div>



<div class="programs-list row">
  <% @programs.each do |program| %>
    <div class="program-card col-md-4 mb-4">
      <div class="card h-100 bg-white rounded-4">
        <div class="card-body">
          <div class="card-title d-flex justify-content-between align-items-center">
            <h3><%= program.title %></h3>
            <!-- You can add an icon or any additional item on the right side of the card title here. -->
          </div>
          <p class="card-text"><%= program.description %></p>
          <ul class="list-unstyled">
            <li><strong>Begin Date:</strong> <%= program.begin_date %></li>
            <li><strong>End Date:</strong> <%= program.end_date %></li>
          </ul>
        </div>
      </div>
    </div>
  <% end %>
</div>
